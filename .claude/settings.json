{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.ts|*.tsx|*.js|*.jsx|*.mjs|*.cjs|*.json|*.css|*.md) npx prettier --write \"$f\" >/dev/null 2>&1; npx eslint --fix \"$f\" >/dev/null 2>&1 ;; esac"
          }
        ]
      }
    ]
  }
}
